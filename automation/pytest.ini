[pytest]
testpaths = tests
addopts =
    -ra
    --strict-markers
    --alluredir=reports/allure-results
markers =
    smoke: core smoke tests
    ui: web UI automation tests
    api: HTTP/API tests

